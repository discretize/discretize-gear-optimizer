name: Prettier Check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - uses: actionsx/prettier@v3
        with:
          # prettier CLI arguments.
          args: --check .
