export const guardian = (theme) => ({
  ...theme,
  name: 'guardian',
  palette: {
    ...theme.palette,
    primary: {
      light: '#a5f4ff',
      main: '#72c1d9',
      dark: '#3d91a7',
    },
  },
});

export const revenant = (theme) => ({
  ...theme,
  name: 'revenant',
  palette: {
    ...theme.palette,
    primary: {
      light: '#ff9e87',
      main: '#d16e5a',
      dark: '#9c4030',
    },
  },
});

export const warrior = (theme) => ({
  ...theme,
  name: 'warrior',
  palette: {
    ...theme.palette,
    primary: {
      light: '#ffff97',
      main: '#ffd166',
      dark: '#c9a036',
    },
  },
});

export const engineer = (theme) => ({
  ...theme,
  name: 'engineer',
  palette: {
    ...theme.palette,
    primary: {
      light: '#ffcd87',
      main: '#d09c59',
      dark: '#9c6e2d',
    },
  },
});

export const ranger = (theme) => ({
  ...theme,
  name: 'ranger',
  palette: {
    ...theme.palette,
    primary: {
      light: '#bfffb2',
      main: '#8CDC82',
      dark: '#5baa54',
    },
  },
});

export const thief = (theme) => ({
  ...theme,
  name: 'thief',
  palette: {
    ...theme.palette,
    primary: {
      light: '#f3bfc5',
      main: '#c08f95',
      dark: '#8f6167',
    },
  },
});

export const elementalist = (theme) => ({
  ...theme,
  name: 'elementalist',
  palette: {
    ...theme.palette,
    primary: {
      light: '#ffbbb7',
      main: '#f68a87',
      dark: '#c05b5a',
    },
  },
});

export const mesmer = (theme) => ({
  ...theme,
  name: 'mesmer',
  palette: {
    ...theme.palette,
    primary: {
      light: '#e9a9ff',
      main: '#b679d5',
      dark: '#854ba3',
    },
  },
});

export const necromancer = (theme) => ({
  ...theme,
  name: 'necromancer',
  palette: {
    ...theme.palette,
    primary: {
      light: '#83d99e',
      main: '#52a76f',
      dark: '#1d7743',
    },
  },
});

export const boon = (theme) => ({
  ...theme,
  name: 'boon',
  palette: {
    ...theme.palette,
    primary: {
      light: '#ffebaa',
      main: '#e5b97a',
      dark: '#b1894d',
    },
  },
});

export const condition = (theme) => ({
  ...theme,
  name: 'condition',
  palette: {
    ...theme.palette,
    primary: {
      light: '#ff6a5c',
      main: '#ea3131',
      dark: '#b00009',
    },
  },
});

export const control = (theme) => ({
  ...theme,
  name: 'control',
  palette: {
    ...theme.palette,
    primary: {
      light: '#ffffff',
      main: '#ecdfd7',
      dark: '#baada6',
    },
  },
});

export const effect = (theme) => ({
  ...theme,
  name: 'effect',
  palette: {
    ...theme.palette,
    primary: {
      light: '#c8ffff',
      main: '#93ffec',
      dark: '#5ecbba',
    },
  },
});

export default {
  guardian,
  revenant,
  warrior,
  engineer,
  ranger,
  thief,
  elementalist,
  mesmer,
  necromancer,
  boon,
  condition,
  control,
  effect,
};
